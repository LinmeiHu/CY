**工程 Bug 修复与验证 · 2026-09-08**

本轮围绕当前工程的执行、资金账户、资金缩放、缓存与验收入口修复了 19 类问题。开始工作时已有 15 个跟踪文件被修改，且资金缩放目录尚未提交；这些工作已保留。另一个任务同时在此目录推进研究，下面的结论以测试源码快照为准。

最终验证结果：**233 passed，1 skipped，0 failures，0 errors**。测试耗时 71.93 秒；测试期间 119 个源码/配置文件的 SHA256 一致，交付前再次核对也一致。静态检查无错误，`git diff --check` 通过。另有一个来自历史冻结缺陷展示用例的 pandas FutureWarning，未修改冻结 alpha 来隐藏警告。

| # | 问题与影响 | 修复及回归验证 |
|---|---|---|
| 1 | 默认测试遗漏研究模块，环境还会导入另一个旧目录的包，可能出现“测绿了错误源码”。 | pytest 固定当前 `src` 和工程根目录，默认包含当前共享账户与资金缩放测试；本次另行运行整个 `tests research`。 |
| 2 | 空信号、没有合法入场或全部尚未退出时，outcome 缺少日期与退出字段，发生 KeyError。 | 两种 outcome 生产器使用完整表结构；原研究补丁改为复用已修好的公共实现。覆盖空集合、无入场、未完成持仓六种情况。 |
| 3 | 没有成交或没有拒单时，回放输出成为零列表，无法稳定导出和校验。 | 保留输入身份字段及成交、拒绝字段；现金账户可以输出空 Parquet 表。 |
| 4 | 共享路由器默认在最后一笔已完成交易处截断，遗漏仍持有头寸的后续净值。 | 有未退出头寸时持续估值到已提供行情尾部；显式结束日期仍优先。 |
| 5 | 缺失决策/执行时间、未来执行订单可能绕过比较；缩放入口还绕过原账户校验。 | 共用无副作用的订单校验，先验证时间、尺寸、价格单位和合法策略，再改变状态。 |
| 6 | 无效时间导致操作报错之前，现金、已处理身份或价格单位已经变化。 | 在买入、卖出、现金事件与价格更新的修改动作之前校验时间；测试确认失败后账户完全不变。 |
| 7 | 只配置最大持仓数时与 `None` 比较崩溃；只配置日入场数时限制被忽略。 | 两项容量限制各自生效。 |
| 8 | 缩放可重新资助已完成事件，或纳入未选择的策略；空/重复策略列表也可进入初始化。 | 阻止事件重放，验证策略集合、优先策略与模式，在入口拒绝非法请求。 |
| 9 | 原生请求会覆盖登记报价中的禁止买入标志，造成不合法成交。 | 已有报价和交易限制优先保留；禁止买入用例保持零成交。 |
| 10 | 已完成的固定股数目标仍留在 pending，形成错误的未完成订单与重复检查。 | 股数已相等时立即移除，细小浮点残差在可用报价下完成核对。 |
| 11 | Gap 退出遇到当天新增、不可卖库存后，后续不再调度卖出；其他事件触发时还可能使用旧退出价。 | 保留真实库存，后续合法报价时重试；测试中原退出价 11、下一合法价 12，最终按 12 卖出。 |
| 12 | ETF 原生目标归零时直接返回，已有缩放持仓没有卖出。 | 归零目标进入实际清仓路径，仍遵守原有成交量和费用约束。 |
| 13 | 现金恰好足够一手 ETF 时，归一化及 `nextafter` 的浮点误差可能把 100 股变成 0 股。 | 整手边界只校正求解精度，并严格核对实际现金支出；恰好足够买 100 股，少 0.01 元则不买。 |
| 14 | 受锁定库存影响而偏离目标权重时，报告仍固定写相对权重误差为零。 | 按实际根事件持仓和原生目标权重计算偏差，锁定库存反例能显示真实偏差。 |
| 15 | 基准现金、NAV 或 exposure 含 NaN 时，误差比较可能错误通过。 | 在差值比较前拒绝空表和非有限账户值。 |
| 16 | 不完整缓存回执、错误场景身份可漏检；中断后重跑又可能把可变摘要纳入不可变哈希。 | 回放与最终验收共用回执检查，核对完整核心文件、场景与状态；只封存不可变账户文件，摘要可重新生成。 |
| 17 | 部分缓存没有绑定实际生产源码、执行路径或初始状态；仅依据旧 PASS 可能复用不兼容 Native 结果。 | 扩充源码/初始状态身份，Native 复用比较实际 Native 生产源码；主入口先验证输入，失败刷新状态，并保留运行中漂移检查。 |
| 18 | 纯现金账户的事件归因表没有行时，`nlargest`/`nsmallest` 对 object 列崩溃。 | 明确数值列类型；真实保存、再次读取和重算摘要的用例验证 NAV 4000、P&L 0 均一致。 |
| 19 | Parquet 对比的主键或数值列含空格、双引号时，SQL 引用不完整，比较崩溃。 | 统一引用并转义字段，包括首个差异的主键定位；异常字段名可以正确返回差异。 |

本任务新增的回归用例位于 `research/capital_scaling_v1/tests/test_bugfix_regressions.py`、`test_finalize_receipts.py`、`research/shared_capital_v1/tests/test_account_guards.py`、`tests/unit/test_compare.py` 与 `tests/unit/test_execution.py`。同时验证了工程已有及另一任务新增的测试，包括整手边界和实际持仓延迟卖出。

复跑全工程测试：

```bash
cd /Users/linmei/Documents/CY-worktrees/five-strategy-capital-scaling-v1
/opt/anaconda3/bin/python -m pytest -q tests research --durations=10 --junitxml=reports/engineering_bugfix_tests_20260908.xml
```

解释器与依赖使用本机已有且符合 `requirements.lock` 的环境：Python 3.13、NumPy 2.1.3、pandas 2.2.3、DuckDB 1.5.2、PyArrow 19.0.0、pytest 8.3.4。CLI 冒烟检查通过；未重新配置用户的全局 Python。

冻结策略目录、原始源码、冻结配置和 manifests 与当前 HEAD 没有差异。公共执行模块的修正另有 `execution_hardening_v1.json` 登记；没有改写冻结 alpha 或股票池。静态检查只排除了由 `frozen_namespace` 注入平台 API 的 `smv6_frozen.py`，其余代码完成语法和未定义名称检查。

一个全量 MCB 外部数据复现测试因没有设置 `FIVE_STRATEGY_INPUT_CONFIG` 跳过。这不表示底层数据不存在，也不表示完整策略回测已经复现。测试中的真实数据切片、公司行动、停复牌、连续账户和无融资约束均已纳入本轮验证。

历史冻结实现中已有的缺陷展示用例仍保留，当前研究执行走已修正的适配器。原生平台等价性、市场容量建模以及完整 132 场景重新计算属于另外的研究证据；本报告不把旧回测结果当成本次修复后的新结果，也不宣称任意未来修改都没有 Bug。

机器可核对的证据为同目录的 `engineering_bugfix_tests_20260908.xml`、`engineering_bugfix_source_snapshot_20260908.json` 和 `engineering_bugfix_static_20260908.json`。源码快照记录测试前后的逐文件 SHA256 与实际命令；本次未提交或推送代码。

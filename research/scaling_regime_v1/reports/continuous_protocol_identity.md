# 连续账户身份闭合

ROLLFORWARD_IDENTITY_STATUS = PASS。权威协议 `AUTHORITATIVE_CONTINUOUS_PROTOCOL_V1`，SHA256 `f194822e1aa73037c0b2694e8484cc55a04197141e79106009641fff698874ec`。协议在本次完整连续 Native 运行之前冻结，以源码和历史前缀为依据。

2018 年只载入一次父研究原始边界状态。ATRDR/MCB 带入 2014—2017 原生连续账户产生的现金、持仓与活动事件；Gap 从冻结的 2018 原点开始；SMV6 初始化一次。随后实际现金、持仓、原生冷却/挂起状态、公司行动状态及 ETF 回调状态连续携带。每日原生队列重置和原生退出照常执行，日历年份不触发初始化。行业与市场指标仅使用当时已完成信息，Full Book 仍仅由原生事件触发。

五个历史 Top 组合各 973 日的现金、仓位、净值最大误差 1.081e-7 元，成交及期末持仓通过。新建四种连续 Native 的历史前缀与父账户精确一致。独立运行 2021、2022、2023、2024、2025 年末截止版本，与 2026-09-04 完整回放的早期数值、成交、意图、持仓和完整 ETF callback context 对照全部通过。ATRDR/MCB 还在六个截止日之前实际截断日线，重新计算特征与信号，记录一致。Gap 使用绑定的冻结信号中间物和实际账户意图前缀验证，不能将这个等级宣称为完整分钟原始数据再生产。

MCB 原快照 ID 是原 full_market 生产器写入的 manifest 数据集身份，不是由行业名称推测的 ID。已核对原 manifest、inventory 和原始行业文件。原规则 `industry_snapshot_id IS NOT NULL` 已恢复，14,626 个候选行的谓词对照见明细。还修复了 2026-08-14 起 59,392 行行业分类源切换，并重建相关特征。原 Eastmoney 快照截至 2026-08-13，后续使用同生产器向后 as-of 携带规则，不宣称取得之后的新行业披露。

分段比较另行保存。独立 MCB 模式的 2022 年开账 NAV 差为 SMV6 延续的 260,255.978086 元；其他股票策略的连续状态一致。confirmation_tag 模式另外保留了此前过滤后的实际 MCB 资金，2022 年开账 MCB NAV 比父分段重新载入的独立 Native 状态少 377,365.302307 元。2022/2023 年的 MCB P&L 差分别为 -715.795043/-16,360.626146 元。此差异落在冻结前已声明的“连续实际模式状态 vs 独立原生分段快照”定义内。费用已包含在 P&L 中，不能再作为第二个可加项重复计入。

行业/IFCGR 维持 PIT-B 等级，不升级为严格历史披露档案。MCB 使用父研究冻结实现，沿用其既有生产器来源等级。SMV6 仍是本地物理账户回调实现，未证明原生 SuperMind 平台完全等价。本协议的 live 一词定义连续状态语义，不等于实盘就绪。

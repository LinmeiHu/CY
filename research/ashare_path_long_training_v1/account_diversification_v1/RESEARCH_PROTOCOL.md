# Account formation V1 — frozen before new account outcomes

2020–2023 development only, PIT-B. Preserve every active training process and the original resource clock (2026-09-10T01:20:45Z, original 48/96h). No retraining or new held-out data. Seed17 is the first complete four-year model set, chosen for availability before outcomes; replicate other seeds after their original queue completes.

## Prediction admission
Original development grid has 97 dates, all exactly10 trading sessions apart, first2020-01-07(global index3166). Anchor k uses calendar indices `(t-3166)%10==k`, clipped to2020–2023, including legal early-January dates. Offset0 must reproduce the original grid exactly. Daily eligibility uses the last historical membership snapshot at or before decision, and the identical current safe/history>=60 conditions. This is the same carried membership convention already used by the frozen daily industry feature, not a new current-universe backfill. New listings may wait until the next snapshot (max9sessions); disclose this inherited sparse-membership boundary. Do not refresh industry peers from future membership.

Use frozen yearly32k checkpoint and frozen M0_CONTEXT residual offset. Recompute the exact causal float16 raw/leg encodings and float32 factors without labels. Compare ALL original-grid candidates: exact stored features, M0 offset maxabs<=2e-7, neural prediction maxabs<=2e-6 (CPU/MPS numeric tolerance already used by this project). Require identical candidate keys and preserve all predictions before positive-return filtering. A mismatch blocks accounts; never loosen tolerance or fall back silently. Shard by stock for bounded memory and interrupt recovery. CPU inference uses one thread while original MPS queue runs.

## P0 / anchor
Original cash_engine.py unchanged: initial1m, positive Ret20, rank pred20 then stock index, max10 physical stocks,10% decisionNAV/new stock, original20-session expiry, original CA operational exit and actual next-open execution. Ten offsets change dates only. Never promote the best phase. Report distributions, correlations and peak/trough coincidence, with original phase marked.

## P1 physical account with virtual lots
Daily new reservations including fees<=5% close NAV; each of up to10 new lots<=0.5% NAV. Unused lot budgets stay cash. Each lot has its own signal, entry, scheduled20-session expiry and actual executions; same stock may recur. Physical name count may exceed10 (the old10-name limit would defeat the requested20-cohort experiment); maximum10 new lots/day. Execution delays can make more than20 cohorts remain live. All lots share ONE cash balance and action receivable/tax ledger.

Aggregate physical stock cap10% decisionNAV includes all available/pending quantities and new reservations. Size using conservative next-open price cap for all existing shares in that name; no sale proceeds assumed available at decision. At fill recheck aggregate stock value against10% decisionNAV using observed open, cancel whole new order if breached. Market price drift can exceed10% afterward; report breaches and admit no further buys, do not invent forced exits. This is an admission cap, not a promise of continuous capped MTM weights. No financing; exact Decimal cash/share/NAV conservation.

One new order per stock/day. Aggregate due sell quantities per physical stock/day, charge one historical physical-order commission and allocate total fee across lots proportionally with exact remainder assigned deterministically to last lot (no tolerance relaxation). Lot exit ages preserved. Retain known CA next-open operational exits, rights ownership, pending tradability, dividends and holding-period tax; unknown held facts block replay rather than invent timing. All inventory and cash movements carry lot identity plus physical-stock aggregation. Use actual minimumcommission, tax, rounding, capacity, suspensions/limits and rejections.

First validate baseline mode daily ledgers EXACTLY against existing P0, then stagger. No new risk model, filters or stops in P1. Independent ledger verifier must reconcile per-lot, per-stock and whole cash daily, plus drawdown endpoint cash-flow bridge. CA and partial expiry fixtures required.

## Decision gates and continuation
Compare full four-year paths and individual years, same-average-exposure constant-cap P0 (<=0.2pp average exposure error, at most6 bisection iterations solely on exposure, never return/DD). If P1 exceeds maximum legal baseline exposure, report matching infeasible under the contract rather than add leverage. No success claim without feasible matched control. No daily-sleeve scan. Pareto is joint CAGR/DD with per-year/turnover/exposure and material-alpha-loss disclosure; a nondominated point alone is not target attainment.

P1 complete ->2022 commonDD/cohort-age/stock exact attribution -> risk prediction diagnostics -> only supported common new-risk budget -> marginal-risk tie-break/coarse utility. Preserve old20d expiry; slow new risk only. Conditional seed disagreement is independent and not inferred from low name overlap. Warning layer requires full PIT coverage. Each stage writes ACCOUNT_FORMATION_STATE/ACCOUNT_PARETO and result-driven next reason; no automatic unsupported policy promotion. Network Pareto and queue remain separate. Reuse existing cy agent watchdog for implementation beyond finite local rules; do not call a finite runner a general research agent.

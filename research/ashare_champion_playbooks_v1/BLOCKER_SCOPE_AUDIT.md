# Blocker scope audit

- P1/P4 company actions: original universe had 1,149 missing share-credit events across 730 symbols. P3B backfilled one official fact; 1,148 remain. Potential 60-session signal paths are 576 for P1 and 2,902 for P4. Classification: `REUSABLE_DATA_GAP`.
- P2: raw data and 14:25 prefix producer exist; 48 monthly inputs and the prior 53-check qualification are reusable. Missing layer is `PLAYBOOK_ADMISSION_NOT_MECHANIZED`, specifically the P2 five-day base and same-clock activity contract. Status remains `BLOCKED_MINUTE_DATA` until its dedicated adapter passes.
- P3A: exact-purpose PIT first-public catalyst permission and revision history remain absent: `BLOCKED_EVENT_DATA`.
- P3B: signal implementation and native account gaps were finite and are now closed.
- P4: the continuation request's contextual-leader identity requires PIT sector/earnings/catalyst context absent from the frozen chart-only proxy: `BLOCKED_CONTEXT_DATA`.

# EXP-CBC-002 clean preregistration

EXP-CBC-002 is the clean execution identity for unchanged H-025 after CBC-001's
implementation-only call-signature failure. It inherits the full scientific
contract from CBC-001 exactly.

The sole correction is explicit `extra_controls=()` in the already-intended
controlled estimator. No feature, weight, sample, endpoint, control, metric,
direction, gate, falsification, timing rule, or interpretation changed. CBC-001
remains permanently invalid and has no accepted result.

CBC-002 uses fresh `_v2` output paths and revalidates all scientific inputs plus
every one of 81 registered CY-011 files. The 220-row 2020-2023 discovery sample
and complete exclusion of locked 2024-2026 values remain unchanged.

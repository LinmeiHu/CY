# EXP-CBC-001 preregistration — chip-base coherence

H-025 tests one PIT-B discovery mechanism: a narrow retained central cost base
combined with upward marginal average-cost migration may represent latent demand
absorbing supply without dispersing established holders, permitting greater
subsequent MFE.

The sole primary feature is the equal-weight mean of within-entry-year percentile
ranks of `-i90_width_pct`, `i90_base_retention`, and
`average_cost_delta / cyqk_close_pre`. The primary endpoint is continuous accepted
MFE. I70, MFE>=20%, and non-false-breakout are fixed attacks, not alternatives.

The sample is exactly 220 accepted 2020-2023 signal-date entries, all with one
strict, hard-valid CY-011 row available at 15:30 and complete fixed controls.
Every one of 81 registered CY-011 files must match its frozen size and SHA-256
before execution. No 2024-2026 row may be materialized.

The frozen tests cover four LOYO omissions, two temporal blocks, H-004 breadth,
daily V1/market/beta/liquidity state, I70, duration/exit, Top-4 PnL, severe-loss,
security, industry, and component coherence. Exact gates and decisions are in
`experiments/EXP-CBC-001_spec.json`.

This is exploratory PIT-B discovery on consumed outcomes. No threshold, filter,
signal, replay, ranking, sizing, entry, exit, or strategy action is authorized.
If every gate passes, the decision is only `VALIDATE`, and the run must stop
before accessing the locked 2024-2026 partition.

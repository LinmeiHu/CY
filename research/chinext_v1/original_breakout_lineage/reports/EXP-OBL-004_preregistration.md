# EXP-OBL-004 preregistration

This is the first outcome reveal for
`LINEAGE-OBL-003-4193834A6A3A39BF`. The four assignments, names, features,
normalization, and split are immutable.

The fixed primary predictor is ordinal lineage strength: L00=0, L01/L10=1, and
L11=2. The two co-primary outcomes are continuous MFE and the accepted
non-false-breakout indicator, both expected positive. BH correction applies over
exactly those two raw tests. Both endpoints must pass raw and controlled gates;
terminal return and right-tail summaries cannot rescue failure.

Controls, year effects, LOYO, three temporal blocks, the frozen five-minute/base
neighbor, Top-1%/extreme-winner/severe-loss removal, duration/exit sensitivity,
and security/industry leave-out attacks are fixed in the experiment spec.

This reveal is exploratory bounded PIT-B evidence on historically consumed data.
No V1 rule or CY-011 access is authorized regardless of the result.

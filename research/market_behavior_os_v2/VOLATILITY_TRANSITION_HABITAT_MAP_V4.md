# Continuous volatility transition and habitat map V4

MKT-VOL-TRANS-004 is an output-schema-only retry of the final supported
scientific/control design in MKT-VOL-TRANS-003 spec `a90dd17f...`.

The 003 complete support audit passed and the frozen estimators ran. During
report serialization, the renderer requested `hashes.spec_sha256` while the
result object exposed the same parent identity as `hashes.scientific_spec_sha256`.
Execution stopped before a complete three-artifact output was accepted. The
partial panel/result are unaccepted and must not be cited or inspected for
frontier decisions.

The only 004 correction is to expose the already-bound scientific-spec hash
under the renderer's expected alias and to use 004 output identities. No input,
row, response, estimator, grouping, control, coordinate, habitat, block,
threshold, support audit, prohibition, or claim changes. This retry is frozen
before any accepted 004 result is produced.

# Industry rank-rotation persistence falsification map

Frozen before MKT-INDRS-ROT-001 constructs delayed responses or alternate
coordinates. This study challenges the sole MKT-INDRS-DYN-001 survivor; it does
not search for another favorable temporal edge.

## Contamination boundary

MKT-INDRS-DYN-001 consumed 2019-2021 discovery and previously untouched
2022-2023 confirmation for immediate Spearman-rank rotation persistence. The
delayed and alternate-definition hypotheses were selected after that result.
Therefore both time blocks are now `CONSUMED_EXPLORATORY_FALSIFICATION` for this
study. Passing cannot become fresh confirmation or authorize a strategy.

No post-2023 partition or locked holdout is opened.

## Fixed falsifications

1. `delayed_spearman_persistence`: current Spearman rotation at `t` predicts
   the primary rotation coordinate at `t+10`. That response compares ranks at
   `t+5` and `t+10`; it shares no rank snapshot with the current `t-5` versus
   `t` block.
2. `kendall_next_block_persistence`: current Kendall rotation predicts Kendall
   rotation at `t+5`.
3. `displacement_next_block_persistence`: current mean absolute percentile-rank
   displacement predicts displacement at `t+5`.

The alternate definitions were preregistered representation neighbors in
MKT-INDRS-001. Their raw paths are already frozen. This study constructs their
coordinate systems with the exact existing semantics: causal trailing-756
percentile after 504 valid observations, same-date view-minus-ALL_A, and
same-date governed-view percentile rank.

## Controls and estimation

Every falsification uses the same three current controls as the original
persistence edge: broad leadership concentration, leave-one-out co-movement,
and volatility change. Partial Spearman, raw/PIT group estimates, pooled
relative estimates, block membership, response availability, and group-sign
rules inherit MKT-INDRS-DYN-001.

The delayed edge uses every tenth valid predictor row for its nonoverlap
diagnostic; adjacent neighbor edges use every fifth. Predictor and response
dates must remain inside the same consumed block.

## Gates

Each replication must independently pass the unchanged magnitude architecture:

- raw median partial rho at least +0.10 in both blocks;
- at least six of eight raw groups positive in both blocks;
- later-block magnitude at least half earlier-block magnitude;
- nonoverlap median partial rho at least +0.08 in both blocks;
- PIT median partial rho at least +0.08 with six of eight positive groups;
- both relative coordinate medians at least +0.05 with both denominator groups
  positive;
- unchanged 150 raw/PIT and 450/600 pooled-relative minimum support.

The rotation-persistence mechanism survives this falsification only if all
three replications pass. A favorable definition or block cannot rescue another.

## Claim boundary

Passing all replications would upgrade the rotation-persistence mechanism only
to `SUPPORTED_EXPLORATORY_CONSUMED_REPLICATION`; it would still require new
independent time before confirmation. It would not establish return prediction,
industry selection, timing, habitat, causality, execution, or a strategy.
Market/stock returns, strategy outcomes, failed roles, failed MA fields, post-
2023 data, and CY-011 are prohibited.

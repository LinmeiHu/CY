# 2020–2023 筹码状态门禁复核（2026-08-22）

## 结论

当前冻结的筹码特征输出通过结构、因果和守恒审计；另发现并修复了研究放宽路径对停牌状态 `0` 的 Python 真值误判。修复后，停牌日可以作为价格桥接记录继续研究状态链，但不会进入严格样本。

## 证据

- 审计命令：`PYTHONPATH=src python scripts/audit_chip_state_features.py --input data/processed/chip_state_features_2018_2026_v1 --output data/audit/cyq_chip_state_features_gate_20260822.json --threads 10`
- 审计结果：`data/audit/cyq_chip_state_features_gate_20260822.json`
- 结果：`PASS`；9,421,907 行，唯一键通过；筹码质量行 9,340,542；质量行最大 `mass_sum` 误差 `2.9960478542534474e-12`；时间可用性、快照一致性、严格样本逻辑均通过。

## 修复

`scripts/build_chip_state_features.py` 中所有停牌判断改为显式检查 `trade_status is not None and int(trade_status) == 0`，避免 `0 or -1` 把停牌误判为缺失状态。

定向样本：`000004.SZ`，2022-05-05。该日 OHLC 有效、交易状态为停牌、成交量和成交额为空；研究输出标记 `research_suspension_bridge=true`，`mass_sum=1.0`，`strict_sample=false`。这符合研究版和严格版的不同边界。

## 当前限制

冻结输出 `chip_state_features_2018_2026_v1` 是历史版本，尚未重新生成包含新增研究字段的 2020–2023 研究版；因此本证据只证明已有输出审计通过及停牌桥接修复已由短样本验证，不代表策略研究或回测已完成。

## 2020–2023 特征门禁复核

- 审计命令：`PYTHONPATH=src python scripts/audit_chapter8_9_chip_truth_v01.py --out data/audit/chapter8_9_chip_truth_2020_2023.json`
- 结果文件：`data/audit/chapter8_9_chip_truth_2020_2023.json`
- 命令退出码：`0`；结论：`PASS`。
- 覆盖：4,400,570 行、5,247 个标的；严格样本 4,041,730 行。
- 严格样本的时间穿越、快照缺失、筹码守恒、分位数单调性、获利/套牢比例和集中度/空间范围异常均为 `0`。
- 另有 30,072 行阻塞样本缺少分钟快照；它们未进入严格样本，单独记录为 `blocked_snapshot_missing`，不能被解释为严格特征质量通过。
- 原审计脚本曾把“阻塞行缺快照”和“严格样本缺快照”混为一个失败项，且把有效行计数当作失败条件；本轮已修正为分层门禁，避免误报。
- 严格样本中 `ASR`、`space20`、`cyc5/13/34`、`cys13/34` 和 `rpy2` 空值均为 `0`；筹码迁移派生特征没有因计算失败而系统性缺失。

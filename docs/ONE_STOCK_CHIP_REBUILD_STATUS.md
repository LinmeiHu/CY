# 单股筹码重建完成状态

更新时间：2026-08-23

## 已完成，不得重复

- 标的：`000001.SZ`
- 区间：2020-01-02 至 2026-08-12；2018–2019 仅作预热。
- 输出：`data/processed/benchmark_chip_000001_v11_20260823/`
- 规模：7 个年度分区、1,602 个交易日、3 个卖方模型、4,806 个“日期×模型”状态行、53MB；不是 4,806 个交易日，也不是 4,806 条坏数据。
- 代码已实现：跨年迁移、T+1、经济成本坐标、公司行动库存事件、结构主峰、紧凑迁移算子、锚点血缘回放。
- 严格与研究语义已分开：`hard_valid` 不容忍未知历史成本；`research_valid` 仅容忍显式的 `UNKNOWN_COST_*`，不容忍日线回退或其他数据异常。
- 最终结果：每个状态行仍有非零但持续衰减的初始未知成本残量，因此严格口径为 `hard_valid=0/4806`；研究口径为 `research_valid=4806/4806`。`UNKNOWN_COST_PRESENT=4806` 只是诊断标签出现次数，绝不等于 4806 个坏行或不可用行；研究失效行数为 0。这不是行情缺失或重建失败，而是严格 PIT 与有偏研究的资格分层。
- T+1：最大当日再卖出为 0。
- 质量守恒：最大误差为 0。
- 分钟回退：0 行。
- 定向测试：`python -m pytest -q tests/test_real_chip_storage.py tests/test_chip_lineage_resolver.py`，24 passed，退出码 0。
- 已修复按需加载缓存：先查询单年后再查询跨年，不会再因符号缓存只含首个年度而错误返回 `None`。
- 真实血缘回放已通过：
  - 2020-03-06 → 2020-04-03：三模型保留率区间 `[0.885227057, 0.896579581]`。
  - 2020-12-15 → 2021-01-15：跨年区间 `[0.865398814, 0.876321555]`。
  - 2020-03-06 → 2026-08-12：长期区间 `[0.000132130, 0.022070527]`；三模型分歧被保留，没有压成伪精确单值。
- 固定一周研究口径验证已通过：`000001.SZ`、2020-06-12 至 2020-06-19、18 个“日期×模型”状态行；`strict_valid_rows=0`、`research_valid_rows=18`。T+1、质量守恒、真实分钟输入、未知成本显式保留和血缘回放全部 PASS。报告：`output/real_chip_week_validation_000001_v11.json`。
- 一周验证接口已修复：读取 v11 的 `research_valid` 作为研究门禁，同时保留源数据 `hard_valid=false`，没有把研究资格冒充为严格 PIT。
- 当前定向测试：`python -m pytest -q tests/test_chip_week_validation.py tests/test_real_chip_storage.py tests/test_chip_lineage_resolver.py`，26 passed，退出码 0；相关 `ruff` 检查退出码 0。

## 未完成的下一步

只用本输出对 `000001.SZ` 跑固定一周的实际策略生命周期；筹码存储、有效性选择和血缘回放的一周验证已经完成，不得重新运行。完成前不扩大股票范围、不跑年度策略研究、不重新审核底层数据。
